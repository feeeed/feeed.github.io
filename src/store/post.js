    const state = () => ({
        count: 0,
        name: 'Eduardo'
    })

    const getters: {
        doubleCount: (state) => state.count * 2,
    },
    const actions: {
        increment() {
            this.count++
        },
    },
})